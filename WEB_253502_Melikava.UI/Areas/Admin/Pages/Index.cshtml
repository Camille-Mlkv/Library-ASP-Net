@page
@model WEB_253502_Melikava.UI.Areas.Admin.Pages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Collection</h1>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Название</th>
            <th>Автор</th>
            <th>Цена</th>
            <th>Картинка</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var book in Model.Books.Items)
        {
            <tr>
                <td>@book.Title</td>
                <td>@book.Description</td>
                <td>@book.Price.ToString("C")</td>
                <td>
                    <img src="@book.Image" alt="Картинка книги" style="max-width: 100px; max-height: 150px;" />
                </td>
                <td>
                    <a href="@Url.Page("/Edit", new { id = book.Id })" class="btn btn-primary">
                        <i class="bi bi-pencil"></i> Edit
                    </a>
                    <a href="@Url.Page("/Details", new { id = book.Id })" class="btn btn-info">
                        <i class="bi bi-info-circle"></i> Details
                    </a>
                    <a href="@Url.Page("/Delete", new { id = book.Id })" class="btn btn-danger">
                        <i class="bi bi-trash"></i> Delete
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

<!-- Пагинация -->
<nav aria-label="Pagination">
    <ul class="pagination">
        <li class="page-item @(Model.CurrentPage == 1 ? "disabled" : "")">
            <a class="page-link" href="?pageNumber=@(Model.CurrentPage - 1)">Предыдущая</a>
        </li>

        @for (int i = 1; i <= Model.TotalPages; i++)
        {
            <li class="page-item @(Model.CurrentPage == i ? "active" : "")">
                <a class="page-link" href="?pageNumber=@i">@i</a>
            </li>
        }

        <li class="page-item @(Model.CurrentPage == Model.TotalPages ? "disabled" : "")">
            <a class="page-link" href="?pageNumber=@(Model.CurrentPage + 1)">Следующая</a>
        </li>
    </ul>
</nav>